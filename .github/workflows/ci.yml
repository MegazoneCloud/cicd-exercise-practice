name: CI

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm test
  build-and-push:
    needs: test
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}
      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            seongje00416/hello-server-for-cicd-test-1126:latest
            seongje00416/hello-server-for-cicd-test-1126:${{ github.sha }}
  deploy:
    needs: build-and-push
    runs-on: self-hosted
    steps:
      - name: Deploy new image
        run: |
          docker pull seongje00416/hello-server-for-cicd-test-1126:latest
          docker stop my-app || true
          docker rm my-app || true
          docker run -d --name my-app -p 3000:3000 seongje00416/hello-server-for-cicd-test-1126:latest